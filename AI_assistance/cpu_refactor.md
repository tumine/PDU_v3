根据当前的代码仓库状态，结合以下信息，完成开发需求：
1. 实验要求位于 `Lab6 - CODH 2026.html`，初步接入 Cache 的 CPU 的完整实现位于 `vsrc/PDU/CPU/your_cpu` 目录下
2. 现有的 PDU 框架内置的存储器没有读写延迟，因此与 Cache 接入场景不适配
3. 首先阅读现有的 PDU 实现，分析 PDU 控制 CPU 的完整流程；阅读 CPU 实现，了解当前 CPU 的工作流程和实现逻辑、以及 PDU 如何通过各种控制信号控制 CPU 的行为
4. 再分析为了将 `n_way_cache.v` 中实现的 cache 模块接入 CPU，需要对于整个 your_cpu 目录下的各个模块分别作出什么改动
5. 之后分析为了让 PDU 能用于测试带 Cache 的 CPU 的实现效果，需要如何修改 CPU 和 PDU 之间的接口通信配置
6. 将以上所有分析内容、详细的代码修改和重构方案写入 `AI_assistance` 目录中的一个 markdown 文件，要求具体到每个模块分别需要修改什么内容，修改后期望实现什么功能更新
7. 你可以修改的内容包括 PDU 实现、CPU 实现和 Cache 实现。对于其它必要的修改内容，请在 markdown 文件中额外分析需要对其进行修改的必要性。

---

Gemini 3.1 Pro 润色后：
# 提示词：基于 AI 的带 Cache CPU 及其测试框架（PDU）重构指南

## 1. 角色定位 (Role Definition)
你是一位精通计算机体系结构、硬件描述语言（Verilog/SystemVerilog）以及 FPGA 验证框架设计的资深硬件架构师。你对 RISC-V/ARM 流水线 CPU 设计、多路组相联 Cache 状态机及其写入策略（如 Write-Back/Write-Through）、以及测试总线/调试单元（PDU）的握手与阻塞机制有深刻的理解。

## 2. 任务背景与核心目标 (Context & Objectives)
当前任务是指导或自动完成一个数字化逻辑实验仓库的重构。目标是将一个多路组相联 Cache 模块（`n_way_cache.v`）接入现有的流水线 CPU 之中，并同步重构现有的外围测试/调试框架（PDU），使其能够正确处理并测试存在读写延迟（Latency/Stall）的存储系统。

**关键已知条件与技术痛点：**
1. **实验依据与源码目录**：核心实验需求位于 `Lab6 - CODH 2026.html`。初步接入 Cache 的 CPU 完整实现代码位于 `vsrc/PDU/CPU/your_cpu` 目录。
2. **核心痛点**：现有的 PDU 框架内置的存储器为**零延迟（Zero-Delay）**理想存储器。而接入 Cache 后，Cache Miss 会引发多个时钟周期的挂起或主存读写等待，导致原有的 PDU 控制流与当前的流水线状态、内存访问时序产生严重冲突（不适配）。

## 3. 详细执行步骤与工作流 (Workflow Steps)

请严格按照以下步骤对代码仓库进行增量式、系统性的分析与重构：

### 第一步：现有架构与控制流分析 (Codebase Review & Flow Analysis)
1. **PDU 深度剖析**：仔细阅读现有的 PDU 实现，梳理出 PDU 控制 CPU 运行（如单步执行 `step`、连续运行 `run`、暂停、读写寄存器、读写内存）的完整状态机及信号交互流程。
2. **CPU 控制机理分析**：阅读 `your_cpu` 目录下的所有模块，分析 CPU 当前的流水线工作流程、停顿（Stall）逻辑、前推（Forwarding）逻辑，以及 PDU 的各种控制信号是如何切入并控制 CPU 行为的。

### 第二步：Cache 模块接入方案设计 (CPU-Cache Integration)
1. **流水线阻塞机制改造**：分析为了将 `n_way_cache.v` 接入 CPU 的指令/数据访问阶段（如 IF 或 MEM 阶段），CPU 的整体流水线控制器（Pipeline Controller）需要做出何种调整。重点关注当 Cache Miss 发生时，如何通过生成 `stall` 信号挂起整条流水线（保持 PC、流水线寄存器不变），直到 Cache 响应 `ready`。
2. **模块级变更分析**：逐一审查 `your_cpu` 目录下的各个子模块（如 `PC`, `IF`, `ID`, `EX`, `MEM`, `WB` 等），明确说明为了适配 Cache 的 Ready/Valid 握手信号或 Busy/Stall 信号，各模块分别需要修改哪些输入输出接口、内部寄存器和组合逻辑。

### 第三步：PDU 测试框架适配与通信重构 (PDU-CPU Interface Adaptation)
1. **延迟自适应改造**：由于现有 PDU 无法处理内存读写延迟，请设计一种机制（例如引入握手应答信号、等待主存/Cache 准备就绪的 `ready` 状态等），重构 CPU 与 PDU 之间的接口通信配置，使其能够支持带延迟的内存操作。
2. **调试通路适配**：确保在 PDU 进行内存察看（Memory Inspection）或寄存器察看时，若数据需要经过 Cache，不会因为 Cache 的内部状态（如 Dirty 块写回或 Miss 触发主存读）而导致 PDU 的状态机死锁或数据读取错误。

### 第四步：输出重构方案报告 (Refactoring Documentation)
请将你所有的分析过程、详细的代码修改和重构方案写入 `AI_assistance` 目录中的一个 Markdown 文件中（推荐命名为 `Refactor_Plan.md`）。

## 4. 方案报告格式与规范 (Output Document Standards)
输出的重构方案必须详尽、具体，禁止宽泛的概括。格式要求如下：
- **模块化展开**：必须针对 `your_cpu` 里的每一个 Verilog 模块，采用 `### [模块名]` 的三级标题形式进行结构化列举。
- **结构化内容**：每个模块内部必须明确包含以下两个部分：
  - **具体修改内容**：清晰列出哪些输入输出端口（Ports）、内部寄存器（Regs）、线网（Wires）以及状态机分支需要修改，并给出关键的 Verilog 代码片段或修改伪代码。
  - **期望实现的功能更新**：详细说明修改后该模块如何响应 Cache 延迟、如何与 PDU 信号对齐，以及最终期望达到的功能行为。
- **边界声明与额外分析**：你的修改权限包含 **PDU 实现**、**CPU 实现** 和 **Cache 实现**。若在分析中发现必须修改除此之外的其他模块（例如全局时钟复位管理、顶层仿真外设等），必须在报告中开辟“**外围模块修改必要性分析**”专栏，详述改动该模块的绝对必要性、改动点以及潜在的风险。

## 5. 提示词约束项与防错提示 (Constraints & Validations)
- **防止组合逻辑环路**：在重构流水线暂停逻辑、PDU 挂起逻辑与 Cache Ready 信号时，严禁引入组合逻辑环路（Combinational Loops）。
- **时序对齐**：确保状态机在 Miss -> Memory Access -> Refill -> Hit 转换过程中，与流水线 Stall 信号的时序严格对齐，防止出现流水线多挂起一个周期或提前释放导致数据错漏的问题。
- **无破坏性改动**：在引入 Cache 机制的同时，不得破坏 CPU 原有的指令集（如 RV32I/M）执行正确性以及 PDU 原有的基本调试控制边界。

---

根据当前的代码仓库状态，结合以下信息，完成开发需求：
1. 实验要求位于 `Lab6 - CODH 2026.html`，初步接入 Cache 的 CPU 的完整实现位于 `vsrc/PDU/CPU/your_cpu` 目录下。
2. 现有的 PDU 框架内置的存储器没有读写延迟，因此与 Cache 接入场景不适配。
3. 首先阅读现有的 PDU 实现，分析 PDU 控制 CPU 的完整流程；阅读 CPU 实现，了解当前 CPU 的工作流程和实现逻辑、以及 PDU 如何通过各种控制信号控制 CPU 的行为。
4. 再分析为了将 `n_way_cache.v` 中实现的 cache 模块接入 CPU，需要对于整个 your_cpu 目录下的各个模块分别作出什么改动。
5. 之后分析为了让 PDU 能用于测试带 Cache 的 CPU 的实现效果，需要如何修改 CPU 和 PDU 之间的接口通信配置。
6. 基于以上所有分析内容，拟定一个详细的代码修改和重构方案写入 `AI_assistance` 目录中的一个 markdown 文件，要求具体到每个模块分别需要修改什么内容，修改后期望实现什么功能更新。
7. 写入完整的修改方案后，开始实际修改代码仓库，注意对于改动的代码附加中文注释解释其功能。在修改完成后，再创建一个 markdown 文件描述完成的工作，包括对什么文件做了什么修改。
8. 你可以修改的内容包括 PDU 实现、CPU 实现和 Cache 实现。对于其它必要的修改内容，请在 markdown 文件中额外分析需要对其进行修改的必要性。

---

Gemini 3.1 Pro 润色后：
# 角色与目标
你是一个精通计算机体系结构、Verilog HDL 以及 FPGA 软硬件协同调试的资深 AI 硬件工程师。
你的任务是基于当前代码仓库的状态，协助我完成带 Cache 的 CPU 模块接入、以及配套测试框架的深度代码重构。

# 上下文信息
- **实验指导书**：详细要求请参考 `Lab6 - CODH 2026.html`。
- **当前进度**：初步接入 Cache 的 CPU 完整实现代码位于 `vsrc/PDU/CPU/your_cpu` 目录。
- **核心痛点**：现有的 PDU (Program Debug Unit) 框架内置的存储器没有读写延迟，这与接入 Cache 后的握手时序和阻塞场景完全不适配。需要对代码进行从下到上的重构。

# 执行步骤
请严格按照以下四个阶段依序执行，切勿跳步：

## 第一阶段：系统级时序与逻辑分析 (Analysis)
1. **PDU 控制流分析**：阅读现有的 PDU 实现，梳理 PDU 控制 CPU 的完整流程、状态机流转逻辑。
2. **CPU 内部与接口分析**：阅读 CPU 现有实现，理解当前 CPU 的数据通路、工作流程，并精准定位 PDU 是如何通过各类控制信号介入并控制 CPU 行为的。
3. **Cache 模块适配分析**：阅读 `n_way_cache.v`，分析为了将该 Cache 模块正确接入 CPU，`your_cpu` 目录下的各个子模块（如取指、访存、流水线控制/停顿机制等）分别需要做怎样的时序和逻辑调整。
4. **测试接口重构分析**：为了使 PDU 能够正确测试带有访存延迟（Cache 命中/缺失）的 CPU，评估并分析 CPU 与 PDU 之间的接口通信配置需要进行哪些针对性的修改。

## 第二阶段：输出详细重构方案 (Planning)
基于第一阶段的分析，在 `AI_assistance` 目录下生成一份详细的 Markdown 文档（建议命名为 `Refactor_Plan.md`）。该文档必须包含：
1. **模块级修改清单**：具体到每个模块（文件），详细说明需要修改的端口、状态机或时序逻辑。
2. **期望效果**：清晰描述每个修改点在重构后期望实现的功能更新。
3. **越界修改论证**：你的常规修改权限仅限 PDU 实现、CPU 实现和 Cache 实现。若评估后认为必须修改这三者之外的依赖文件，必须在此文档中单辟一节，详细论证对其进行修改的必要性。

## 第三阶段：执行代码重构 (Execution)
在完整写入并输出修改方案后，开始对代码仓库执行实质性的代码修改：
1. 严格按照 `Refactor_Plan.md` 中的设计落地代码。
2. **代码注释强制要求**：对于所有被改动、重构或新增的代码逻辑，**必须**在代码旁附加详尽的中文注释，解释其对应的功能和时序握手机制。

## 第四阶段：收尾与总结 (Review)
所有代码修改和注释均完成后，在 `AI_assistance` 目录下创建第二份 Markdown 文档（建议命名为 `Execution_Summary.md`），用于记录本次重构的执行情况。文档需包含：
1. 本次重构实际完成的工作总结。
2. 修改清单：精确列出对哪些文件做了哪些具体的修改（diff 简述）。

---

在vivado中进行综合时，提示查找表使用数量过多，具体情况如图所示。
1. 删除现有CPU中ALU对于乘除法运算指令的支持。
2. Cache中只保留LRU替换策略。
3. 分析是否还有其它可行方案来减少查找表数量。
4. 拟定一个可行的实施方案输出到AI_assistance目录下的markdown文件
5. 写入完整的实施方案后，开始实际修改代码仓库，注意对于改动的代码附加中文注释解释其功能。在修改完成后，再创建一个 markdown 文件描述完成的工作，包括对什么文件做了什么修改。

---

Gemini 3.1 Pro 润色后：
# 角色与目标
你是一个精通 FPGA 开发、Vivado 综合优化以及计算机体系结构的资深硬件工程专家。
当前项目在 Vivado 中进行综合（Synthesis）时遭遇了**查找表（LUT）资源使用数量过多（严重超额/拥堵）**的瓶颈。你的任务是协助我通过“裁剪冗余逻辑”和“架构级轻量化”来深度重构代码仓库，大幅降低 LUT 的资源消耗。

# 核心重构任务
请围绕“降低 LUT 资源占用”这一核心目标，对现有 CPU 和 Cache 模块进行以下实质性重构：

1. **ALU 乘除法功能裁剪**：
   - 彻底删除现有 CPU 中 ALU 对于乘法和除法运算指令（例如 RISC-V M 扩展指令）的硬件支持，将其退化为仅支持基础算术与位运算的轻量化 ALU。
   - 清理或移除与之相关的硬件乘法器/除法器 IP、多周期控制状态机或暂存逻辑，以释放大量 LUT 资源。

2. **Cache 替换策略精简**：
   - 对 Cache 模块的替换算法进行瘦身，**仅保留纯粹的 LRU（Least Recently Used）替换策略**。
   - 移除其他可能存在的复杂替换策略（如伪随机、PLRU 等多策略切换控制电路），精简编码寄存器和状态更新树，从而降低控制逻辑对 LUT 的消耗。

# 执行步骤
请严格按照以下四个阶段依序执行，切勿跳步：

## 第一阶段：资源瓶颈分析与拓展探索 (Analysis & Exploration)
1. **基础裁剪评估**：分析上述两项裁剪具体涉及哪些 Verilog 文件，预估其在去除多周期或组合逻辑乘除法后能释放的逻辑资源。
2. **多维优化方案探索**：除上述两项明确的裁剪要求外，深度分析当前代码仓库，**探索是否还有其它切实可行的方案来进一步减少 LUT 的数量**。
   - *提示视角*：检查是否存在大位宽的多路选择器（MUX）可进行结构拆解、状态机编码是否可优化（如 One-hot 与 Binary 切换）、寄存器堆（Regfile）或存储器接口是否可以通过改用 Block RAM（BRAM）代替分布式 RAM 从而腾出 LUT 空间等。

## 第二阶段：撰写实施方案文档 (Planning)
基于第一阶段的分析与探索，在 `AI_assistance` 目录下创建一份 Markdown 文档 `LUT_Optimization_Plan.md`。该文档必须包含：
1. **拟定重构清单**：明确指出需要修改、裁剪或重构的模块、具体文件及逻辑变动点。
2. **拓展方案论证**：详细阐述你在第一阶段探索出的“其它可行方案”及其必要性、实施可行性与预期资源收益。
3. **预期效果**：清晰描述各项修改后，对 Vivado 综合结果（LUT 占用率）的期望改善目标。

## 第三阶段：代码实质性重构 (Execution)
在完整输出并确认实施方案后，开始对代码仓库执行实质性的修改：
1. 严格按照 `LUT_Optimization_Plan.md` 中的规划修改 CPU、ALU 和 Cache 的 Verilog 源码。
2. **代码注释强制要求**：对于所有被改动、裁剪、重构或优化的硬件逻辑，**必须**在代码旁附加详尽的**中文注释**，清晰解释其功能改变、信号裁剪逻辑及资源优化意图。

## 第四阶段：收尾与盘点 (Review)
所有代码修改、Vivado 重新综合验证及注释均完成后，在 `AI_assistance` 目录下创建第二份 Markdown 文档 `LUT_Optimization_Summary.md`，用于记录本次重构的执行情况。文档需包含：
1. 本次资源优化实际完成的工作总结。
2. 修改清单：精确列出对哪些文件做了哪些具体的修改（diff 简述），并对比重构前后代码逻辑与资源占用的变化。
