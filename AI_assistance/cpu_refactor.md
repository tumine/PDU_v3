根据当前的代码仓库状态，结合以下信息，完成开发需求：
1. 实验要求位于 `Lab6 - CODH 2026.html`，初步接入 Cache 的 CPU 的完整实现位于 `vsrc/PDU/CPU/your_cpu` 目录下
2. 现有的 PDU 框架内置的存储器没有读写延迟，因此与 Cache 接入场景不适配
3. 首先阅读现有的 PDU 实现，分析 PDU 控制 CPU 的完整流程；阅读 CPU 实现，了解当前 CPU 的工作流程和实现逻辑、以及 PDU 如何通过各种控制信号控制 CPU 的行为
4. 再分析为了将 `n_way_cache.v` 中实现的 cache 模块接入 CPU，需要对于整个 your_cpu 目录下的各个模块分别作出什么改动
5. 之后分析为了让 PDU 能用于测试带 Cache 的 CPU 的实现效果，需要如何修改 CPU 和 PDU 之间的接口通信配置
6. 将以上所有分析内容、详细的代码修改和重构方案写入 `AI_assistance` 目录中的一个 markdown 文件，要求具体到每个模块分别需要修改什么内容，修改后期望实现什么功能更新
7. 你可以修改的内容包括 PDU 实现、CPU 实现和 Cache 实现。对于其它必要的修改内容，请在 markdown 文件中额外分析需要对其进行修改的必要性。

---

Gemini 3.1 Pro 润色后：
# 提示词：基于 AI 的带 Cache CPU 及其测试框架（PDU）重构指南

## 1. 角色定位 (Role Definition)
你是一位精通计算机体系结构、硬件描述语言（Verilog/SystemVerilog）以及 FPGA 验证框架设计的资深硬件架构师。你对 RISC-V/ARM 流水线 CPU 设计、多路组相联 Cache 状态机及其写入策略（如 Write-Back/Write-Through）、以及测试总线/调试单元（PDU）的握手与阻塞机制有深刻的理解。

## 2. 任务背景与核心目标 (Context & Objectives)
当前任务是指导或自动完成一个数字化逻辑实验仓库的重构。目标是将一个多路组相联 Cache 模块（`n_way_cache.v`）接入现有的流水线 CPU 之中，并同步重构现有的外围测试/调试框架（PDU），使其能够正确处理并测试存在读写延迟（Latency/Stall）的存储系统。

**关键已知条件与技术痛点：**
1. **实验依据与源码目录**：核心实验需求位于 `Lab6 - CODH 2026.html`。初步接入 Cache 的 CPU 完整实现代码位于 `vsrc/PDU/CPU/your_cpu` 目录。
2. **核心痛点**：现有的 PDU 框架内置的存储器为**零延迟（Zero-Delay）**理想存储器。而接入 Cache 后，Cache Miss 会引发多个时钟周期的挂起或主存读写等待，导致原有的 PDU 控制流与当前的流水线状态、内存访问时序产生严重冲突（不适配）。

## 3. 详细执行步骤与工作流 (Workflow Steps)

请严格按照以下步骤对代码仓库进行增量式、系统性的分析与重构：

### 第一步：现有架构与控制流分析 (Codebase Review & Flow Analysis)
1. **PDU 深度剖析**：仔细阅读现有的 PDU 实现，梳理出 PDU 控制 CPU 运行（如单步执行 `step`、连续运行 `run`、暂停、读写寄存器、读写内存）的完整状态机及信号交互流程。
2. **CPU 控制机理分析**：阅读 `your_cpu` 目录下的所有模块，分析 CPU 当前的流水线工作流程、停顿（Stall）逻辑、前推（Forwarding）逻辑，以及 PDU 的各种控制信号是如何切入并控制 CPU 行为的。

### 第二步：Cache 模块接入方案设计 (CPU-Cache Integration)
1. **流水线阻塞机制改造**：分析为了将 `n_way_cache.v` 接入 CPU 的指令/数据访问阶段（如 IF 或 MEM 阶段），CPU 的整体流水线控制器（Pipeline Controller）需要做出何种调整。重点关注当 Cache Miss 发生时，如何通过生成 `stall` 信号挂起整条流水线（保持 PC、流水线寄存器不变），直到 Cache 响应 `ready`。
2. **模块级变更分析**：逐一审查 `your_cpu` 目录下的各个子模块（如 `PC`, `IF`, `ID`, `EX`, `MEM`, `WB` 等），明确说明为了适配 Cache 的 Ready/Valid 握手信号或 Busy/Stall 信号，各模块分别需要修改哪些输入输出接口、内部寄存器和组合逻辑。

### 第三步：PDU 测试框架适配与通信重构 (PDU-CPU Interface Adaptation)
1. **延迟自适应改造**：由于现有 PDU 无法处理内存读写延迟，请设计一种机制（例如引入握手应答信号、等待主存/Cache 准备就绪的 `ready` 状态等），重构 CPU 与 PDU 之间的接口通信配置，使其能够支持带延迟的内存操作。
2. **调试通路适配**：确保在 PDU 进行内存察看（Memory Inspection）或寄存器察看时，若数据需要经过 Cache，不会因为 Cache 的内部状态（如 Dirty 块写回或 Miss 触发主存读）而导致 PDU 的状态机死锁或数据读取错误。

### 第四步：输出重构方案报告 (Refactoring Documentation)
请将你所有的分析过程、详细的代码修改和重构方案写入 `AI_assistance` 目录中的一个 Markdown 文件中（推荐命名为 `Refactor_Plan.md`）。

## 4. 方案报告格式与规范 (Output Document Standards)
输出的重构方案必须详尽、具体，禁止宽泛的概括。格式要求如下：
- **模块化展开**：必须针对 `your_cpu` 里的每一个 Verilog 模块，采用 `### [模块名]` 的三级标题形式进行结构化列举。
- **结构化内容**：每个模块内部必须明确包含以下两个部分：
  - **具体修改内容**：清晰列出哪些输入输出端口（Ports）、内部寄存器（Regs）、线网（Wires）以及状态机分支需要修改，并给出关键的 Verilog 代码片段或修改伪代码。
  - **期望实现的功能更新**：详细说明修改后该模块如何响应 Cache 延迟、如何与 PDU 信号对齐，以及最终期望达到的功能行为。
- **边界声明与额外分析**：你的修改权限包含 **PDU 实现**、**CPU 实现** 和 **Cache 实现**。若在分析中发现必须修改除此之外的其他模块（例如全局时钟复位管理、顶层仿真外设等），必须在报告中开辟“**外围模块修改必要性分析**”专栏，详述改动该模块的绝对必要性、改动点以及潜在的风险。

## 5. 提示词约束项与防错提示 (Constraints & Validations)
- **防止组合逻辑环路**：在重构流水线暂停逻辑、PDU 挂起逻辑与 Cache Ready 信号时，严禁引入组合逻辑环路（Combinational Loops）。
- **时序对齐**：确保状态机在 Miss -> Memory Access -> Refill -> Hit 转换过程中，与流水线 Stall 信号的时序严格对齐，防止出现流水线多挂起一个周期或提前释放导致数据错漏的问题。
- **无破坏性改动**：在引入 Cache 机制的同时，不得破坏 CPU 原有的指令集（如 RV32I/M）执行正确性以及 PDU 原有的基本调试控制边界。

---

根据当前的代码仓库状态，结合以下信息，完成开发需求：
1. 实验要求位于 `Lab6 - CODH 2026.html`，初步接入 Cache 的 CPU 的完整实现位于 `vsrc/PDU/CPU/your_cpu` 目录下。
2. 现有的 PDU 框架内置的存储器没有读写延迟，因此与 Cache 接入场景不适配。
3. 首先阅读现有的 PDU 实现，分析 PDU 控制 CPU 的完整流程；阅读 CPU 实现，了解当前 CPU 的工作流程和实现逻辑、以及 PDU 如何通过各种控制信号控制 CPU 的行为。
4. 再分析为了将 `n_way_cache.v` 中实现的 cache 模块接入 CPU，需要对于整个 your_cpu 目录下的各个模块分别作出什么改动。
5. 之后分析为了让 PDU 能用于测试带 Cache 的 CPU 的实现效果，需要如何修改 CPU 和 PDU 之间的接口通信配置。
6. 基于以上所有分析内容，拟定一个详细的代码修改和重构方案写入 `AI_assistance` 目录中的一个 markdown 文件，要求具体到每个模块分别需要修改什么内容，修改后期望实现什么功能更新。
7. 写入完整的修改方案后，开始实际修改代码仓库，注意对于改动的代码附加中文注释解释其功能。在修改完成后，再创建一个 markdown 文件描述完成的工作，包括对什么文件做了什么修改。
8. 你可以修改的内容包括 PDU 实现、CPU 实现和 Cache 实现。对于其它必要的修改内容，请在 markdown 文件中额外分析需要对其进行修改的必要性。

---

Gemini 3.1 Pro 润色后：
# 角色与目标
你是一个精通计算机体系结构、Verilog HDL 以及 FPGA 软硬件协同调试的资深 AI 硬件工程师。
你的任务是基于当前代码仓库的状态，协助我完成带 Cache 的 CPU 模块接入、以及配套测试框架的深度代码重构。

# 上下文信息
- **实验指导书**：详细要求请参考 `Lab6 - CODH 2026.html`。
- **当前进度**：初步接入 Cache 的 CPU 完整实现代码位于 `vsrc/PDU/CPU/your_cpu` 目录。
- **核心痛点**：现有的 PDU (Program Debug Unit) 框架内置的存储器没有读写延迟，这与接入 Cache 后的握手时序和阻塞场景完全不适配。需要对代码进行从下到上的重构。

# 执行步骤
请严格按照以下四个阶段依序执行，切勿跳步：

## 第一阶段：系统级时序与逻辑分析 (Analysis)
1. **PDU 控制流分析**：阅读现有的 PDU 实现，梳理 PDU 控制 CPU 的完整流程、状态机流转逻辑。
2. **CPU 内部与接口分析**：阅读 CPU 现有实现，理解当前 CPU 的数据通路、工作流程，并精准定位 PDU 是如何通过各类控制信号介入并控制 CPU 行为的。
3. **Cache 模块适配分析**：阅读 `n_way_cache.v`，分析为了将该 Cache 模块正确接入 CPU，`your_cpu` 目录下的各个子模块（如取指、访存、流水线控制/停顿机制等）分别需要做怎样的时序和逻辑调整。
4. **测试接口重构分析**：为了使 PDU 能够正确测试带有访存延迟（Cache 命中/缺失）的 CPU，评估并分析 CPU 与 PDU 之间的接口通信配置需要进行哪些针对性的修改。

## 第二阶段：输出详细重构方案 (Planning)
基于第一阶段的分析，在 `AI_assistance` 目录下生成一份详细的 Markdown 文档（建议命名为 `Refactor_Plan.md`）。该文档必须包含：
1. **模块级修改清单**：具体到每个模块（文件），详细说明需要修改的端口、状态机或时序逻辑。
2. **期望效果**：清晰描述每个修改点在重构后期望实现的功能更新。
3. **越界修改论证**：你的常规修改权限仅限 PDU 实现、CPU 实现和 Cache 实现。若评估后认为必须修改这三者之外的依赖文件，必须在此文档中单辟一节，详细论证对其进行修改的必要性。

## 第三阶段：执行代码重构 (Execution)
在完整写入并输出修改方案后，开始对代码仓库执行实质性的代码修改：
1. 严格按照 `Refactor_Plan.md` 中的设计落地代码。
2. **代码注释强制要求**：对于所有被改动、重构或新增的代码逻辑，**必须**在代码旁附加详尽的中文注释，解释其对应的功能和时序握手机制。

## 第四阶段：收尾与总结 (Review)
所有代码修改和注释均完成后，在 `AI_assistance` 目录下创建第二份 Markdown 文档（建议命名为 `Execution_Summary.md`），用于记录本次重构的执行情况。文档需包含：
1. 本次重构实际完成的工作总结。
2. 修改清单：精确列出对哪些文件做了哪些具体的修改（diff 简述）。

---

在vivado中进行综合时，提示查找表使用数量过多，具体情况如图所示。
1. 删除现有CPU中ALU对于乘除法运算指令的支持。
2. Cache中只保留LRU替换策略。
3. 分析是否还有其它可行方案来减少查找表数量。
4. 拟定一个可行的实施方案输出到AI_assistance目录下的markdown文件
5. 写入完整的实施方案后，开始实际修改代码仓库，注意对于改动的代码附加中文注释解释其功能。在修改完成后，再创建一个 markdown 文件描述完成的工作，包括对什么文件做了什么修改。

---

Gemini 3.1 Pro 润色后：
# 角色与目标
你是一个精通 FPGA 开发、Vivado 综合优化以及计算机体系结构的资深硬件工程专家。
当前项目在 Vivado 中进行综合（Synthesis）时遭遇了**查找表（LUT）资源使用数量过多（严重超额/拥堵）**的瓶颈。你的任务是协助我通过“裁剪冗余逻辑”和“架构级轻量化”来深度重构代码仓库，大幅降低 LUT 的资源消耗。

# 核心重构任务
请围绕“降低 LUT 资源占用”这一核心目标，对现有 CPU 和 Cache 模块进行以下实质性重构：

1. **ALU 乘除法功能裁剪**：
   - 彻底删除现有 CPU 中 ALU 对于乘法和除法运算指令（例如 RISC-V M 扩展指令）的硬件支持，将其退化为仅支持基础算术与位运算的轻量化 ALU。
   - 清理或移除与之相关的硬件乘法器/除法器 IP、多周期控制状态机或暂存逻辑，以释放大量 LUT 资源。

2. **Cache 替换策略精简**：
   - 对 Cache 模块的替换算法进行瘦身，**仅保留纯粹的 LRU（Least Recently Used）替换策略**。
   - 移除其他可能存在的复杂替换策略（如伪随机、PLRU 等多策略切换控制电路），精简编码寄存器和状态更新树，从而降低控制逻辑对 LUT 的消耗。

# 执行步骤
请严格按照以下四个阶段依序执行，切勿跳步：

## 第一阶段：资源瓶颈分析与拓展探索 (Analysis & Exploration)
1. **基础裁剪评估**：分析上述两项裁剪具体涉及哪些 Verilog 文件，预估其在去除多周期或组合逻辑乘除法后能释放的逻辑资源。
2. **多维优化方案探索**：除上述两项明确的裁剪要求外，深度分析当前代码仓库，**探索是否还有其它切实可行的方案来进一步减少 LUT 的数量**。
   - *提示视角*：检查是否存在大位宽的多路选择器（MUX）可进行结构拆解、状态机编码是否可优化（如 One-hot 与 Binary 切换）、寄存器堆（Regfile）或存储器接口是否可以通过改用 Block RAM（BRAM）代替分布式 RAM 从而腾出 LUT 空间等。

## 第二阶段：撰写实施方案文档 (Planning)
基于第一阶段的分析与探索，在 `AI_assistance` 目录下创建一份 Markdown 文档 `LUT_Optimization_Plan.md`。该文档必须包含：
1. **拟定重构清单**：明确指出需要修改、裁剪或重构的模块、具体文件及逻辑变动点。
2. **拓展方案论证**：详细阐述你在第一阶段探索出的“其它可行方案”及其必要性、实施可行性与预期资源收益。
3. **预期效果**：清晰描述各项修改后，对 Vivado 综合结果（LUT 占用率）的期望改善目标。

## 第三阶段：代码实质性重构 (Execution)
在完整输出并确认实施方案后，开始对代码仓库执行实质性的修改：
1. 严格按照 `LUT_Optimization_Plan.md` 中的规划修改 CPU、ALU 和 Cache 的 Verilog 源码。
2. **代码注释强制要求**：对于所有被改动、裁剪、重构或优化的硬件逻辑，**必须**在代码旁附加详尽的**中文注释**，清晰解释其功能改变、信号裁剪逻辑及资源优化意图。

## 第四阶段：收尾与盘点 (Review)
所有代码修改、Vivado 重新综合验证及注释均完成后，在 `AI_assistance` 目录下创建第二份 Markdown 文档 `LUT_Optimization_Summary.md`，用于记录本次重构的执行情况。文档需包含：
1. 本次资源优化实际完成的工作总结。
2. 修改清单：精确列出对哪些文件做了哪些具体的修改（diff 简述），并对比重构前后代码逻辑与资源占用的变化。

---

1. 拟定一个重构DRAM的方案，输出到AI_assistance目录中
2. 写入完整的实施方案后，开始实际修改代码仓库，注意对于改动的代码附加中文注释解释其功能。在修改完成后，再创建一个 markdown 文件描述完成的工作，包括对什么文件做了什么修改。

---

Gemini 3.1 Pro 润色：
# 角色与目标
你是一个精通计算机体系结构、Verilog HDL 及 FPGA 存储器层级设计的资深硬件工程师。
我们正在进行一个带有 Cache 的 CPU 重构项目。当前的任务是接力之前的进度，对数据存储器（DMEM/DRAM）模块进行深度的架构级重构与代码编写。

# 背景与核心约束
- **项目现状**：CPU 已初步接入 Cache，并且前期已经进行了减少查找表（LUT）资源的优化。
- **仿真刚需**：你给出的代码**必须完整实现数据内存阵列（Data Memory Array）**，切勿省略内部存储逻辑或仅给出空壳接口，否则将导致整个 CPU 系统的仿真无法正常运行。
- **功能特性**：在重构 DMEM 时，必须妥善设计读写掩码（Write/Read Mask）与字节选通逻辑，以完美支持处理器的数据读写需求（包括对齐与非对齐内存访问）。
- **资源考量**：在实现存储介质时，请结合 FPGA 资源特性（如考虑使用 BRAM 宏或 IP 替代大规模的纯分布式逻辑），以避免引发 LUT 拥堵。

# 执行步骤
请严格按照以下四个阶段依序执行，切勿跳步：

## 第一阶段：DMEM 架构与接口分析 (Analysis)
1. 分析当前 CPU 及 Cache 模块对下层数据存储器（DMEM）的接口需求（如地址总线位宽、数据位宽、读写使能信号、握手或延迟信号等）。
2. 梳理 DMEM 的内部架构，确定存储器阵列的组织方式（如按字节寻址、字对齐方式等）。
3. 结合之前的 LUT 优化需求，评估 DMEM 最优的实现方式（例如推断 BRAM 的 Verilog 写法）。

## 第二阶段：撰写重构方案文档 (Planning)
基于第一阶段的分析，在 `AI_assistance` 目录下创建一份 Markdown 文档（建议命名为 `DMEM_Refactor_Plan.md`）。该文档必须包含：
1. **接口定义与时序说明**：明确重构后 DMEM 模块的输入输出端口及其握手时序。
2. **存储器实现策略**：详细说明将如何编写内存阵列代码以保证仿真可行性，以及如何处理字节选通逻辑。
3. **关联模块修改评估**：如果在重构 DMEM 时需要微调 Cache 到 DMEM 的接口代码，请在此列出修改计划。

## 第三阶段：代码实质性重构 (Execution)
在完整输出并确认实施方案后，开始对代码仓库执行实质性的修改：
1. 严格按照 `DMEM_Refactor_Plan.md` 编写或重写 DMEM（及相关顶层连线）的 Verilog 源码。
2. **强制要求**：确保数据内存被切实实现并可用于仿真环境。
3. **代码注释强制要求**：对于所有改动或新增的存储器逻辑，**必须**在代码旁附加详尽的**中文注释**，解释其功能、选通逻辑和时序关系。

## 第四阶段：收尾与盘点 (Review)
所有代码修改及注释均完成后，在 `AI_assistance` 目录下创建第二份 Markdown 文档（建议命名为 `DMEM_Execution_Summary.md`），用于记录本次重构的执行情况。文档需包含：
1. 本次 DMEM 重构实际完成的工作总结。
2. 修改清单：精确列出对哪些文件做了哪些具体的修改（diff 简述）。

---

根据当前的代码仓库状态，结合以下信息，完成开发需求：
1. 实验要求位于`Lab6 - CODH 2026.html`，接入Cache的CPU完整实现位于`vsrc/PDU/CPU/your_cpu`目录下。
2. 首先阅读现有的 PDU 实现，分析 PDU 控制 CPU 的完整流程；阅读 CPU 实现，了解当前 CPU 的工作流程和实现逻辑、以及 PDU 如何通过各种控制信号控制 CPU 的行为。
3. 需要实现基于局部历史的分支预测：设计 BHT 与 PHT 的结构，并接入流水线 CPU。正确修改预测失败后流水线与分支预测器的相关内容。
4. 需要实现基于全局历史的分支预测以及竞争处理：设计全局预测器的结构，并设计算法处理全局预测与局部预测结果不一致时的竞争关系，以及对于全局预测器的实时修正。
5. 基于以上所有分析内容，拟定一个详细的代码修改和重构方案写入`AI_assistance`目录中的一个markdown文件，要求具体到每个模块分别需要修改什么内容，修改后期望实现什么功能更新。
6. 用户确认和修改代码方案后，开始实际修改代码仓库，注意对于改动的代码附加中文注释解释其功能。在修改完成后，再创建一个markdown文件描述完成的工作，包括对什么文件做了什么修改。
7. 注意实现分支预测的代码时，必须检查是否与现有的CPU各模块时序存在冲突，如果出现冲突必须进行解决。

---

Gemini 3.1 Pro 润色：
# 角色与目标
你是一个精通计算机体系结构、Verilog HDL 高级流水线设计以及分支预测算法的资深硬件工程师。
我们正在进行一个 RISC-V CPU 的增量开发项目（当前 CPU 已初步接入 Cache）。本次对话的核心任务是：为流水线 CPU 引入完整的**竞争型分支预测器（Tournament Branch Predictor）**，包含局部预测、全局预测及其竞争选择逻辑，并完美解决与现有流水线的时序耦合问题。

# 背景与核心要求
- **实验上下文**：具体实验要求见 `Lab6 - CODH 2026.html`。CPU 的核心代码位于 `vsrc/PDU/CPU/your_cpu` 目录下。
- **PDU 兼容性**：必须优先阅读现有的 PDU（Program Debug Unit）实现。深入理解 PDU 控制 CPU 的完整握手流程、状态流转逻辑，以及 PDU 是如何通过控制信号干预 CPU 行为的，确保引入分支预测后 PDU 仍能正常调试和接管 CPU。
- **时序与冲突绝对红线**：实现分支预测时，必须极其严谨地检查预测逻辑（IF 阶段的 PC 生成）、预测验证逻辑（EX 或其它解析阶段）、状态更新逻辑与现有 CPU 流水线时序是否存在冲突。如果发生分支预测失败（Misprediction），必须正确实现流水线冲刷（Flush）与正确 PC 恢复，绝不能引入死锁或结构冲突。

# 执行步骤
请严格按照以下四个阶段依序执行。**注意：在完成第二阶段后，必须暂停操作，等待我的确认后才能进入第三阶段。**

## 第一阶段：架构与流水线时序分析 (Analysis)
1. **控制流分析**：通读 PDU 与 CPU 实现，理清现有的取指（IF）、译码/执行（ID/EX）流程，明确当前分支指令解析的阶段和时序。
2. **局部历史预测器设计**：设计 BHT (Branch History Table) 与 PHT (Pattern History Table) 的级联结构。明确如何利用当前 PC 索引 BHT，再由 BHT 状态索引 PHT 给出局部预测。
3. **全局历史预测器设计**：设计 GHR (Global History Register) 与全局 PHT。
4. **竞争选择器设计**：设计 Choice Predictor 结构（如两位饱和计数器阵列），制定局部与全局预测结果不一致时的仲裁策略。
5. **状态更新与恢复机制**：明确在分支实际解析后，如何将真实跳转结果回传，以更新 BHT、GHR、各类 PHT 以及 Choice Predictor 的状态机。

## 第二阶段：输出详细重构方案 (Planning)
基于第一阶段的分析，在 `AI_assistance` 目录下创建一份 Markdown 文档 `Branch_Predictor_Plan.md`。文档必须包含：
1. **模块结构与接口定义**：预测器各子模块（Local, Global, Choice）的 Verilog 接口与内部状态机设计。
2. **流水线改造清单**：详细列出 `your_cpu` 目录下需要修改的模块。重点阐述：
   - 预测失败时的流水线冲刷（Flush）信号生成与分发逻辑。
   - 分支预测器状态的实时修正机制。
   - PDU 控制接口的适配（如果需要）。
3. **时序冲突解决预案**：明确指出预测器可能引入的时序冲突，并给出解决思路。

**【系统中断】：在此阶段完成后，请停止回复，并询问我：“重构方案已生成，请检查 `Branch_Predictor_Plan.md`。是否确认根据此方案开始修改代码？” 等待我的明确同意。**

## 第三阶段：代码实质性重构 (Execution)
（仅在获得我的确认后执行）
1. 严格按照方案进行 Verilog 代码开发，修改相关模块并将分支预测器接入流水线。
2. 密切关注时序对齐，确保寄存器同步更新和组合逻辑的无毛刺评估。
3. **代码注释强制要求**：对于预测逻辑、流水线冲刷控制、状态更新等关键路径，**必须**附加详尽的中文注释，说明其对应的分支预测算法细节和时序关系。

## 第四阶段：收尾与盘点 (Review)
所有代码修改及注释均完成后，在 `AI_assistance` 目录下创建 Markdown 文档 `Branch_Predictor_Summary.md`，用于记录执行情况：
1. 本次分支预测器开发实际完成的工作总结。
2. 修改清单：精确列出对哪些文件做了哪些具体的修改（diff 简述）。

---

为了确定分支预测的效果，需要在PDU中引入新命令和branch_predictor禁用功能。
1. 首先阅读现有的 PDU 实现，分析 PDU 控制 CPU 的完整流程；PDU 如何通过各种控制信号控制 CPU 的行为。
2. 阅读仓库内上一次提交记录中对CPU执行分支跳转指令的控制流所做的修改，分析在没有分支预测的情况下CPU如何执行分支跳转指令、如何进行指令冲刷。
3. 确定PDU的计时器开始和结束计时的时间点，计时单位按时钟周期计算。
4. 设计一个合理的PDU计时单元并接入现有PDU，并通过一个新的命令暴露在串口上。
5. 需要给branch_predictor引入一个禁用功能，在禁用branch_predictor时CPU不执行分支预测。
6. 基于以上所有分析和设计内容，拟定一个详细的代码修改和重构方案写入`AI_assistance`目录中的一个markdown文件，要求具体到每个模块分别需要修改什么内容，修改后期望实现什么功能更新。
7. 用户确认和修改代码方案后，开始实际修改代码仓库，注意对于改动的代码附加中文注释解释其功能。在修改完成后，再创建一个markdown文件描述完成的工作，包括对什么文件做了什么修改。
8. 注意实现新增功能代码时，必须检查是否与现有的CPU各模块时序存在冲突，如果出现冲突必须进行解决。

---

Gemini 3.1 Pro 润色：
# 角色与目标
你是一个精通 RISC-V 体系结构、Verilog HDL 高级流水线控制以及 Vivado 软硬件协同调试的资深硬件工程师。
我们当前的项目是一个带有 Cache 和分支预测器的流水线 CPU。本次对话的核心任务是：为系统引入**性能基准测试（Benchmarking）能力**。具体而言，需要在 PDU（Program Debug Unit）中集成一个精确到时钟周期的计时器，并为 CPU 添加一个可动态开启/关闭分支预测器的硬连线旁路（Bypass），以便通过控制变量法评估分支预测带来的性能收益。

# 背景与核心约束
- **控制流回退分析**：在禁用分支预测时，CPU 必须安全回退到无预测状态。你需要主动检索代码仓库的上一次提交记录，精准分析在引入分支预测之前，CPU 是如何在执行阶段（EX）处理分支跳转指令并进行流水线冲刷（Flush）的。
- **计时器精度红线**：PDU 计时器的 Start 和 Stop 触发条件必须极其严谨。必须明确界定“程序开始执行的第一条指令”到“程序最后一条指令完全退休（Retire）”的准确时钟周期跨度。
- **时序安全**：引入禁用旁路和计时器时，必须绝对保证不与现有的流水线数据通路和握手时序发生冲突，不得引入关键路径延迟（Critical Path Delay）恶化。

# 执行步骤
请严格按照以下四个阶段依序执行。**注意：在完成第二阶段后，必须暂停操作，等待我的确认后才能进入第三阶段。**

## 第一阶段：控制流溯源与计时架构分析 (Analysis)
1. **PDU 交互分析**：阅读现有的 PDU 实现，梳理 PDU 通过各类控制信号接管、启动、暂停 CPU 的完整工作流。
2. **原始分支控制流溯源**：分析上一次提交记录（无分支预测版本），提炼出没有分支预测时，CPU 解析分支指令的时序节点以及对应的指令冲刷机制。
3. **PDU 计时器边界界定**：确定计时器开始计数（Start）和停止计数（Stop）的精确硬件触发条件，单位为系统时钟周期。
4. **禁用逻辑设计**：设计 `branch_predictor_disable` 信号的路由机制，确保该信号拉高时，IF 阶段直接使用 `PC + 4`，并将分支解析权完全交还给后续流水级。

## 第二阶段：输出详细重构方案 (Planning)
基于第一阶段的分析，在 `AI_assistance` 目录下创建一份 Markdown 文档 `Benchmark_Integration_Plan.md`。文档必须包含：
1. **PDU 改造方案**：
   - 计时单元的 Verilog 结构与触发状态机。
   - 新增的 PDU 串口通信命令协议定义（如何通过串口读取运行周期数、如何发送禁用预测指令）。
2. **CPU 旁路改造方案**：
   - `branch_predictor_disable` 信号在 CPU 顶层及流水线内部的布线方案。
   - 禁用时预测器状态的冻结机制（避免脏数据污染历史表）。
3. **潜在冲突排查**：列出可能出现的时序或控制流冲突，并提供解决方案。

**【系统中断】：在此阶段完成后，请停止输出代码，并向我询问：“基准测试方案已生成并写入 `Benchmark_Integration_Plan.md`。是否确认根据此方案开始实际修改代码？” 等待我的明确同意。**

## 第三阶段：代码实质性重构 (Execution)
（仅在获得我的确认后执行代码修改）
1. 严格按照确认后的方案修改 PDU、CPU 顶层及流水线控制模块。
2. 新增一个串口命令用于暴露计时器结果，并实现分支预测的动态开关。
3. **代码注释强制要求**：对于新增的计时逻辑、状态机跳转、串口指令解析以及预测器旁路选择逻辑，**必须**附加详尽的中文注释，说明其在基准测试中的功能。

## 第四阶段：收尾与盘点 (Review)
所有代码修改及注释均完成后，在 `AI_assistance` 目录下创建 Markdown 文档 `Benchmark_Execution_Summary.md`，用于记录执行情况：
1. 本次基准测试功能集成实际完成的工作总结。
2. 修改清单：精确列出对哪些文件做了哪些具体的修改（diff 简述），并说明串口新命令的测试使用方法。

---

现有CPU对于JAL、JALR指令没有执行分支预测。
1. 首先阅读现有的 PDU 实现，分析 PDU 控制 CPU 的完整流程；阅读 CPU 实现，了解当前 CPU 的工作流程和实现逻辑、以及 PDU 如何通过各种控制信号控制 CPU 的行为，特别是如何控制CPU是否使用分支预测模块。
2. 使用BTB用于JAL指令的分支预测。分析如何实现这一功能。
3. 在执行JAL指令时，将其下一条指令地址压入栈结构，并在执行JALR指令时弹出栈顶地址作为预测地址，同时在 BTB 中保存指令类型以方便预测。分析如何实现这一功能
4. 基于以上所有分析和设计内容，拟定一个详细的代码修改和重构方案写入`AI_assistance`目录中的一个markdown文件，要求具体到每个模块分别需要修改什么内容，修改后期望实现什么功能更新。
5. 用户确认和修改代码方案后，开始实际修改代码仓库，注意对于改动的代码附加中文注释解释其功能。在修改完成后，再创建一个markdown文件描述完成的工作，包括对什么文件做了什么修改。
6. 注意实现新增功能代码时，必须检查是否与现有的CPU各模块时序存在冲突，如果出现冲突必须进行解决。
7. 注意新增的分支预测功能需要与现有分支预测功能共享同一个PDU禁用开关。

---

Gemini 3.1 Pro 润色：
# 角色与目标
你是一个精通 RISC-V 体系结构、Verilog HDL 高级流水线设计以及高级分支预测技术的资深硬件工程师。
我们当前的项目是一个带有 Cache 和条件分支预测器的流水线 CPU。本次对话的核心任务是：进一步完善分支预测系统，**新增对 JAL（无条件跳转）和 JALR（间接跳转/函数返回）指令的预测支持**，并确保这些新增的预测单元与现有的预测器完美共享同一个 PDU（Program Debug Unit）禁用开关。

# 背景与核心约束
- **现状分析**：现有 CPU 已经实现了针对条件分支的预测，并通过前序工作引入了来自 PDU 的 `branch_predictor_disable` 禁用信号。但目前 CPU 对于 JAL 和 JALR 指令未执行任何预测。
- **机制引入 (BTB & RAS)**：
  1. **BTB (Branch Target Buffer)**：用于缓存跳转目标地址，并额外**保存指令类型**，以便在取指阶段快速识别出当前 PC 对应的是条件分支、JAL 还是 JALR。
  2. **RAS (Return Address Stack，返回地址栈)**：专用于优化函数调用与返回。在执行 JAL 指令时，将下一条指令的地址（PC+4）压入 RAS；在执行 JALR 指令时，弹出 RAS 栈顶地址作为预测的跳转目标。
- **旁路一致性红线**：新增的 BTB 和 RAS 逻辑必须与现有的分支预测模块**共享同一个 PDU 禁用开关**。当 `branch_predictor_disable` 拉高时，所有预测行为（包括 JAL/JALR 预测、RAS 压栈/出栈）必须完全冻结，CPU 必须安全回退到最原始的指令冲刷与跳转解析逻辑。
- **时序与冲刷安全**：必须严谨处理预测失败时的流水线冲刷（Flush）、正确 PC 恢复以及 RAS 栈指针的修复，绝不能引入死锁或结构冲突。

# 执行步骤
请严格按照以下四个阶段依序执行。**注意：在完成第二阶段后，必须暂停操作，等待我的确认后才能进入第三阶段。**

## 第一阶段：架构分析与控制流梳理 (Analysis)
1. **PDU 交互与禁用逻辑梳理**：阅读现有的 PDU 与 CPU 实现，追踪 `branch_predictor_disable` 信号的布线，明确当前 CPU 是如何关闭分支预测并回退执行的。
2. **BTB 设计与指令类型识别**：设计 BTB 结构。分析如何在 BTB Entry 中增加位宽以保存“指令类型”（如 Branch, JAL, JALR），并在 IF（取指）阶段根据命中 BTB 的类型决定去哪里获取目标 PC（BHT/PHT、BTB 目标字段、还是 RAS）。
3. **RAS 栈结构设计**：设计用于 JALR 的硬件栈机制。明确压栈（Push）和出栈（Pop）触发的时序节点，以及在 JALR 预测失败时如何修正栈顶指针。

## 第二阶段：输出详细重构方案 (Planning)
基于第一阶段的分析，在 `AI_assistance` 目录下创建一份 Markdown 文档 `JAL_JALR_Prediction_Plan.md`。文档必须包含：
1. **硬件结构设计**：详细说明 BTB 的表项结构定义、RAS 的深度与工作原理。
2. **流水线改造清单**：详细列出需要修改的模块。重点阐述：
   - IF 阶段多路目标 PC 的选择逻辑（基于 BTB 提供的指令类型）。
   - EX 阶段真实解析 JAL/JALR 后的验证逻辑、流水线冲刷（Flush）与状态/栈指针恢复机制。
3. **PDU 旁路接入方案**：详细说明如何将 BTB 与 RAS 的工作使能受控于现有的禁用开关。
4. **潜在冲突排查**：列出新增逻辑可能导致的时序冲突（特别是 RAS 在预测错误时的状态污染问题），并给出解决预案。

**【系统中断】：在此阶段完成后，请停止输出代码，并向我询问：“JAL 与 JALR 分支预测方案已生成并写入 `JAL_JALR_Prediction_Plan.md`。是否确认根据此方案开始实际修改代码？” 等待我的明确同意。**

## 第三阶段：代码实质性重构 (Execution)
（仅在获得我的确认后执行代码修改）
1. 严格按照确认后的方案修改 CPU 顶层及流水线各相关模块，实现 BTB 和 RAS。
2. 密切关注时序对齐，确保寄存器同步更新、禁用开关生效以及组合逻辑的无毛刺评估。
3. **代码注释强制要求**：对于 BTB 查找、RAS 压栈/出栈控制、流水线冲刷以及旁路选择逻辑，**必须**附加详尽的中文注释，说明其设计意图和时序关系。

## 第四阶段：收尾与盘点 (Review)
所有代码修改及注释均完成后，在 `AI_assistance` 目录下创建 Markdown 文档 `JAL_JALR_Execution_Summary.md`，用于记录执行情况：
1. 本次新增预测功能实际完成的工作总结。
2. 修改清单：精确列出对哪些文件做了哪些具体的修改（diff 简述）。
